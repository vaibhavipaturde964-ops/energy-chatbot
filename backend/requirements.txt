# EcoBot — Backend dependencies for Render
# Streamlit is NOT included here — this is the FastAPI backend only.

# --- Web framework ---
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# --- Environment ---
python-dotenv>=1.0.0

# --- LLM / Groq ---
groq>=0.9.0

# --- RAG / LangChain ---
langchain>=0.2.0
langchain-community>=0.2.0
langchain-chroma>=0.1.2
langchain-huggingface>=0.0.3
langchain-text-splitters>=0.2.0

# --- Vector database ---
chromadb>=0.5.0

# --- Embeddings ---
sentence-transformers>=3.0.0
torch>=2.2.0
torchvision>=0.17.0

# --- Data loading (used by ingestion, referenced at query time) ---
pandas>=2.0.0
pypdf>=4.0.0
beautifulsoup4>=4.12.0

# --- Validation ---
pydantic>=2.0.0
